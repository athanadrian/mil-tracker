{
  "name": "mil-tracker",
  "version": "1.0.0",
  "description": "Desktop app for defense equipment & personnel tracking",
  "main": "electron/main.cjs",
  "build": {
    "appId": "com.yourorg.miltracker",
    "productName": "Mil Tracker",
    "directories": {
      "buildResources": "build",
      "output": "dist"
    },
    "win": {
      "target": "nsis",
      "icon": "build/icon.ico"
    },
    "mac": {
      "target": [
        "dmg",
        "zip"
      ],
      "category": "public.app-category.productivity",
      "icon": "build/icon.icns",
      "hardenedRuntime": true
    },
    "asar": false,
    "files": [
      "electron/**/*",
      "package.json",
      ".next/standalone/**/*",
      ".next/static/**/*",
      "public/**/*",
      "data/**/*",
      "node_modules/**/*",
      "node_modules/@prisma/**",
      "node_modules/.prisma/**"
    ],
    "extraResources": [
      {
        "from": "node_modules/.prisma",
        "to": "app/node_modules/.prisma"
      }
    ],
    "extraMetadata": {
      "main": "electron/main.cjs"
    }
  },
  "scripts": {
    "dev": "concurrently -k \"cross-env PORT=5123 HOSTNAME=127.0.0.1 DATABASE_URL=file:./data/mil.db next dev -p 5123\" \"wait-on tcp:5123 && electron .\"",
    "build": "next build",
    "start": "concurrently -k \"cross-env PORT=5123 HOSTNAME=127.0.0.1 DATABASE_URL=file:./data/mil.db next start -p 5123\" \"wait-on tcp:5123 && electron .\"",
    "prisma:generate": "cross-env DATABASE_URL=file:../data/mil.db prisma generate --schema=prisma/schema.prisma",
    "prisma:migrate": "cross-env DATABASE_URL=file:../data/mil.db prisma migrate dev --schema=prisma/schema.prisma",
    "seed": "cross-env DATABASE_URL=file:../data/mil.db tsx prisma/seed.ts",
    "dist:win": "npm run prisma:generate && npm run build && electron-builder --win nsis",
    "dist:win:portable": "npm run prisma:generate && npm run build && electron-builder --win portable",
    "dist:mac": "npm run prisma:generate && npm run build && cross-env CSC_IDENTITY_AUTO=false electron-builder --mac dmg zip",
    "dist:mac:universal": "npm run prisma:generate && npm run build && cross-env CSC_IDENTITY_AUTO=false electron-builder --mac universal",
    "dist:mac:signed": "npm run prisma:generate && npm run build && electron-builder --mac dmg zip",
    "clean": "node -e \"fs=require('fs');['dist','.next'].forEach(d=>{try{fs.rmSync(d,{recursive:true,force:true})}catch{}})\"",
    "dist:win:all": "npm run prisma:generate && npm run clean && npm run build && npm run dist:win"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@prisma/client": "^6.16.2",
    "@radix-ui/react-dialog": "^1.1.15",
    "@radix-ui/react-slot": "^1.2.3",
    "class-variance-authority": "^0.7.1",
    "clsx": "^2.1.1",
    "lucide-react": "^0.544.0",
    "next": "^15.5.3",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "tailwind-merge": "^3.3.1",
    "tailwindcss-animate": "^1.0.7",
    "zod": "^4.1.11"
  },
  "devDependencies": {
    "@types/node": "^24.5.2",
    "@types/react": "^19.1.13",
    "concurrently": "^9.2.1",
    "cross-env": "^10.0.0",
    "electron": "^38.1.2",
    "electron-builder": "^26.0.12",
    "prisma": "^6.16.2",
    "tailwindcss": "^4.1.13",
    "ts-node": "^10.9.2",
    "tsx": "^4.20.5",
    "typescript": "^5.9.2",
    "wait-on": "^9.0.1",
    "rimraf": "^5.0.10"
  }
}
